# Art

Visual assets for the game, split by kind:

- `Materials/` - `.mat` assets and shader graphs.
- `Models/` - 3D meshes and imported model files (`.fbx`, `.blend`, etc.).
- `Textures/` - source images used by materials, UI, or sprites.
