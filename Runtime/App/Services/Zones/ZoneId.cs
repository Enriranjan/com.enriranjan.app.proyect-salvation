namespace EnriRanjan.App.ProyectSalvation
{
    /// <summary>
    /// The four zones of the demo, each with one main puzzle (GDD).
    /// </summary>
    public enum ZoneId
    {
        None = 0,

        /// <summary>Zone 1 — arrival, first puzzle of the demo.</summary>
        Zone1,

        /// <summary>Zone 2 — "The solar condor door" (mirror / light beam alignment).</summary>
        Zone2,

        /// <summary>Zone 3 — "The three key fragments".</summary>
        Zone3,

        /// <summary>Zone 4 — climax, "The library plates".</summary>
        Zone4
    }
}
