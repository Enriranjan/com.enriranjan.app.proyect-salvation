# Prefabs

Reusable GameObject prefabs for the game: views, in-scene actors, and any
other object instantiated at runtime or placed by hand in a scene. Prefabs
that are purely visual variants belong here even if their scripts (Views)
live under `Runtime/Unity/Views`.
