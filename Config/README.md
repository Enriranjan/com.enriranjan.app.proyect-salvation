# Config

ScriptableObject assets holding designer-tunable configuration, consumed by
Providers under `Runtime/Unity` and exposed to the pure `Runtime/App`
services through plain data or interfaces - never by passing the
ScriptableObject itself across the boundary.
